# 빠른 시작 가이드

성능 개선된 AI 서버를 테스트하는 가장 빠른 방법입니다.

## 🚀 5분 안에 시작하기

### 1단계: 가상환경 활성화

```bash
cd apps/ai
source venv/bin/activate
```

프롬프트 앞에 `(venv)`가 표시되면 성공!

```bash
(venv) user@machine:~/recipflash/apps/ai$
```

### 2단계: 패키지 설치 확인 (최초 1회)

```bash
pip install -r requirements.txt
```

이미 설치되어 있다면 스킵됩니다.

### 3단계: 서버 실행

```bash
uvicorn src.main:app --reload --port 8000
```

**예상 출력:**
```
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [12345] using StatReload
INFO:     Started server process [12346]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### 4단계: 새 터미널에서 테스트

**새 터미널을 열고:**

```bash
cd apps/ai
source venv/bin/activate  # 다시 활성화 필요!
python test_performance.py <PDF파일경로>
```

**예시:**
```bash
python test_performance.py ~/Downloads/recipe.pdf
```

---

## 📊 결과 확인

### 테스트 스크립트 출력:
```
============================================================
📄 파일: recipe.pdf
📊 크기: 2.45MB
============================================================

✅ 서버 연결 성공: AI server is running

⏱️  성능 테스트 시작...

============================================================
✅ 테스트 성공!
============================================================
⏱️  총 처리 시간: 32.45초
📋 생성된 메뉴 개수: 47개
⚡ 메뉴당 평균 시간: 0.69초
============================================================

📈 성능 비교:
  변경 전 예상 시간: 180.00초 (순차 처리)
  변경 후 실제 시간: 32.45초 (병렬 처리)
  개선율: 5.55배 빠름
  시간 단축: 147.55초
```

### 서버 터미널 로그:
```
############################################################
[PERF] NEW REQUEST - File type: application/pdf
############################################################

[PERF] File read took 0.05s (size: 2.45MB)
[PERF] Starting PDF to image conversion...
[PERF] PDF to image conversion took 3.21s for 10 pages

============================================================
[PERF] Starting PARALLEL processing of 10 pages
============================================================

[PERF] LLM parsing took 12.34s, parsed 5 menus
[PERF] Translation took 0.00s
...

============================================================
[PERF] SUMMARY (PARALLEL):
[PERF] Total pages: 10
[PERF] Total menus generated: 47
[PERF] Speedup: 5.55x  ← 병렬 처리 효과!
============================================================

[PERF] TOTAL REQUEST TIME: 32.45s
############################################################
```

---

## ⚠️ 자주 발생하는 문제

### 1. `ModuleNotFoundError`

```
ModuleNotFoundError: No module named 'fastapi'
```

**해결책:** 가상환경을 활성화하지 않았습니다.
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### 2. `Connection refused`

```
❌ 서버 연결 실패: Connection refused
```

**해결책:** 서버가 실행되지 않았습니다.
```bash
# 다른 터미널에서
cd apps/ai
source venv/bin/activate
uvicorn src.main:app --reload --port 8000
```

### 3. 가상환경이 비활성화됨

새 터미널을 열 때마다 다시 활성화해야 합니다:
```bash
cd apps/ai
source venv/bin/activate
```

---

## 🎯 다음 단계

### 변경 전/후 비교하고 싶다면:
```bash
# 자세한 가이드 확인
cat HOW_TO_COMPARE.md
```

### 상세한 테스트 가이드:
```bash
cat PERFORMANCE_TEST.md
```

### 앱에서 실제로 테스트:
1. 모바일 앱 실행
2. PDF 업로드
3. 서버 로그에서 `[PERF]` 태그 확인
4. 실제 시간 측정

---

## 💡 유용한 명령어

### 가상환경 비활성화
```bash
deactivate
```

### 서버 중지
```
Ctrl+C
```

### 서버 재시작
```bash
pkill -f "uvicorn.*main:app"
uvicorn src.main:app --reload --port 8000
```

### 실시간 로그 확인
서버 실행 터미널에서 `[PERF]` 태그가 포함된 로그를 확인하세요.

---

## 📝 체크리스트

테스트하기 전에 확인하세요:

- [ ] `cd apps/ai`로 올바른 디렉토리에 있음
- [ ] `source venv/bin/activate`로 가상환경 활성화
- [ ] 프롬프트에 `(venv)` 표시됨
- [ ] 서버가 다른 터미널에서 실행 중
- [ ] 테스트할 PDF 파일 준비됨

모두 체크했다면 시작하세요! 🚀
